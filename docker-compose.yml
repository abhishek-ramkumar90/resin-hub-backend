version: "3.9"

services:

  # ── CoatingBazaar Spring Boot API ──────────────────────────────────────────
  coating-bazaar-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: coating-bazaar-api
    ports:
      - "8081:8081"
    restart: unless-stopped

